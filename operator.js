'use strict';

// 1. String concatenation
console.log('my' + ' cat'); // my cat
console.log('1' + 2); // 12
console.log(`string literals: 1 + 2 = ${1+2}`); // string literals: 1 + 2 = 3

// 2. Numeric operatiors
console.log(1 + 1) // add
console.log(1 - 1) // sub
console.log(1 / 1) // div
console.log(1 * 1) // mul
console.log(1 % 1) // remainder
console.log(1 ** 1) // exponentiation

// 3. Increment and decrement operators
let counter = 2;
const preIncrement = ++counter; // preIncrement = 3, counter = 3
const preIncrement = counter++; // preIncrement = 3, counter = 4

// 4. Assginment operators
let x = 3; let y = 6;
x+=y; // x = x + y

// 5. Comparison operators : <, <=, >, >=

// 6. Logical operators : || (or), && (and), ! (not)

// 7. Equality : ==, !=, ===, !==

// 8. Conditional operators : if, else if, else

// 9. Ternary operators : condition ? value1 : value2;

// 10. Switch statement : switch(state){case 'data': break; defalut: break;}

// 11. Loops : while(state){}, do{}while(state);

// 12. for Loops : for(begin; condition; step){}